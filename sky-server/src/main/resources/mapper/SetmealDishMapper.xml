<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">

    <!-- 根据套餐id获取菜品id -->
    <select id="getSetmealIdByDishId" resultType="java.lang.Long">
        select setmeal_id from setmeal_dish where dish_id in
            <foreach collection="dishIds" item="dishId" separator="," open="(" close=")">
                #{dishId}
            </foreach>

    </select>

    <!-- 新增套餐关联菜品 -->
    <insert id="insertBatch">
        insert into setmeal_dish(setmeal_id, dish_id, name, price, copies) values
            <foreach collection="setmealDishes" separator="," item="dish">
                (#{dish.setmealId},#{dish.dishId},#{dish.name},#{dish.price},#{dish.copies})
            </foreach>
    </insert>

    <!-- 根据套餐id查询套餐与菜品关系 -->
    <select id="getLinkOfSetmeal" resultType="com.sky.entity.SetmealDish">
        select sd.id,sd.setmeal_id,sd.dish_id,d.name,d.price,sd.copies from setmeal_dish sd
            left join dish d on sd.dish_id = d.id
                 where setmeal_id = #{setmealId}
    </select>
</mapper>
