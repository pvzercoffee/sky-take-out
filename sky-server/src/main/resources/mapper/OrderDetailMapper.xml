<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">

    <!-- 批量插入订单明细 -->
    <insert id="insertBatch">
        insert into order_detail(name, image, order_id, dish_id, setmeal_id, dish_flavor,number,amount) VALUES
            <foreach collection="orderDetails" separator="," item="orderDetail">
              (  #{orderDetail.name}, #{orderDetail.image},
            #{orderDetail.orderId}, #{orderDetail.dishId},
            #{orderDetail.setmealId}, #{orderDetail.dishFlavor},
            #{orderDetail.number},#{orderDetail.amount} )
            </foreach>
    </insert>

    <!-- 根据订单id批量查询订单详情 -->
    <select id="queryByOrdersIds" resultType="com.sky.entity.OrderDetail">
        select * from order_detail where order_id in(
            <foreach collection="orderIds" item="orderId" separator=",">
                #{orderId}
            </foreach>
        )
    </select>

    <!-- top10销量统计 -->
    <select id="top10" resultType="com.sky.entity.SalesTop10Report">

        select
            od.name as name,
            sum(od.number) number
        from order_detail od,orders o

        where
            od.order_id = o.id
          and order_time &gt;= #{begin}
          and order_time &lt;= #{end}
          and o.status = 5
        group by od.name
        order by number desc

    </select>
</mapper>
